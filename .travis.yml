os: linux
dist: focal
language: php
php: 7.4
services:
  - docker
  - mysql
env:
  DOCKER_COMPOSE_VERSION: 1.26.0
before_install:
  - sudo rm /usr/local/bin/docker-compose
  - curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  - chmod +x docker-compose
  - sudo mv docker-compose /usr/local/bin
  - docker-compose -v
  - docker-compose up -d
before_script:
  - composer install --no-interaction
script:
  #- ./bin/phpunit
  #- docker exec -it my-running-php bin/console doctrine:database:create
  #- docker exec -it my-running-php bin/console doctrine:migrations:migrate -q
  #- docker exec -it my-running-php bin/console doctrine:fixtures:load -q
  #- docker exec -it my-running-php bin/phpunit
   - docker exec -it my-running-php pwd
   - docker exec -it my-running-php ls
after_script:
  - docker-compose down